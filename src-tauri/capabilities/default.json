{
  "identifier": "default",
  "description": "Default permissions for SSHSpan. Capabilities gate what the WEBVIEW may call over IPC; they do not gate the Rust side. Every entry here is something the shipped renderer actually uses - the dialog grants were removed because every file picker in this app is opened from Rust (app.dialog()), which does not consult the ACL, so granting them only widened what an injected script could do.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-is-visible",
    "core:event:default",
    "core:webview:allow-set-webview-zoom",
    "core:tray:default",
    "core:path:default",
    "core:menu:default",

    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text"
  ]
}
