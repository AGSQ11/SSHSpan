/* SSHSpan embedded icon sprite — single stroke source of truth for every icon in the UI.
   Icons: Lucide (ISC). Inline <svg>s are built from this map so the CSP never needs inline markup
   and every icon inherits currentColor. */
"use strict";
const ICONS = {};
